'use client';

import AgentStatus from '../../components/AgentStatus';

export default function AgentsPage() {
    return (
        <div>
            <h1>Agent Management</h1>
            <p style={{ color: 'var(--text-muted)' }}>Select an agent from the Org Tree or Companies view to manage their settings, model, and VM bindings.</p>
        </div>
    );
}
